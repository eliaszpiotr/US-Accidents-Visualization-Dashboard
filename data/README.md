Full dataset files are not stored in GitHub because of their size.

Download the dataset with:

```bash
python scripts/download_data.py
```

Expected file after download:

```text
data/filtereddata.parquet
```
